- name: Ensure group "webservers" exists
  group:
    name: webservers
    state: present
  tasks:
  - name: Disable Firewall
    service: name=firewalld state=stopped enabled=yes
  - name: Selinux disable
    selinux: state=disabled
  - name: selinux set enforce disable
    command: setenforce 0
